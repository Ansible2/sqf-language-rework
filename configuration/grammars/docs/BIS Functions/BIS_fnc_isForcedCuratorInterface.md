Return if curator interface is forced (user cannot leave it by pressing Zeus key).


---
*Syntaxes:*

[] call `BIS_fnc_isForcedCuratorInterface`

---
*Example 1:*

```sqf
private _isForced = call BIS_fnc_isForcedCuratorInterface;
```